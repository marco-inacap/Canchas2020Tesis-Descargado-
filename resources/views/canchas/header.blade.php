 <header class="container-flex space-between">
        <div class="date">
          <span class="c-gris">aqui hay que poner algo</span>
        </div>
        <div class="post-category">
          <span class="category"><a href="{{route('complejos.show', $cancha->complejo)}}">{{optional($cancha->complejo)->nombre}}</a></span>
        </div>
      </header>